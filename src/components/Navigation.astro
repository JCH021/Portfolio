<!-- Navigation -->
<nav class="nav-container">
    <div class="nav-content">
        <div class="nav-logo">JCH.Portfolio</div>
        
        <!-- Hamburger Menu Button -->
        <button class="hamburger" id="hamburger" aria-label="Toggle menu">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <!-- Navigation Links -->
        <ul class="nav-menu" id="navMenu">
            <li><a href="#Startseite" class="nav-link">Startseite</a></li>
            <li><a href="#AboutMe" class="nav-link">Über mich</a></li>
            <li><a href="#Projekte" class="nav-link">Projekte</a></li>
            <li><a href="#Kontakt" class="nav-link">Kontakt</a></li>
        </ul>
    </div>
</nav>

<style>
/* Navigation Container */
.nav-container {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: linear-gradient(to bottom, rgba(10, 9, 8, 0.95), transparent);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
}

.nav-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2rem 1rem;
    max-width: 1400px;
    margin: 0 auto;
}

/* Logo */
.nav-logo {
    font-family: 'Space Mono', monospace;
    font-weight: bold;
    font-size: 1.25rem;
    background: linear-gradient(to bottom right, #ff6b35, #fdc82f);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    z-index: 1001;
}

/* Desktop Navigation */
.nav-menu {
    display: flex;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
}

.nav-link {
    color: #e5e5e5;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
    font-size: 1rem; 
}

.nav-link::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(to right, #ff6b35, #fdc82f);
    transition: width 0.3s ease;
}

.nav-link:hover {
    color: #ff6b35;
}

.nav-link:hover::after {
    width: 100%;
}

/* Hamburger Menu (hidden on desktop) */
.hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 1001;
}

.hamburger span {
    display: block;
    width: 28px;
    height: 3px;
    background: #ff6b35;
    border-radius: 2px;
    transition: all 0.3s ease;
}

.hamburger.active span:nth-child(1) {
    transform: rotate(45deg) translate(8px, 8px);
}

.hamburger.active span:nth-child(2) {
    opacity: 0;
}

.hamburger.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -7px);
}

/* Tablet */
@media (max-width: 1024px) {
    .nav-content {
        padding: 1.5rem 2rem;
    }

    .nav-menu {
        gap: 1.5rem;
    }
}

/* Mobile */
@media (max-width: 768px) {
    .nav-content {
        padding: 1.5rem 1.5rem;
    }

    .nav-logo {
        font-size: 1.1rem;
    }

    /* Show hamburger */
    .hamburger {
        display: flex;
    }

    /* Hide and transform menu */
    .nav-menu {
        position: fixed;
        top: 0;
        right: -100%;
        height: 100vh;
        width: 70%;
        max-width: 300px;
        flex-direction: column;
        background: rgba(10, 9, 8, 0.98);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        padding: 6rem 2rem 2rem;
        gap: 2rem;
        transition: right 0.4s ease;
        box-shadow: -5px 0 20px rgba(0, 0, 0, 0.3);
    }

    .nav-menu.active {
        right: 0;
    }

    .nav-link {
        font-size: 1.1rem;
        padding: 0.5rem 0;
    }
}

/* Small Mobile */
@media (max-width: 480px) {
    .nav-content {
        padding: 1rem 1rem;
    }

    .nav-logo {
        font-size: 1rem;
    }

    .nav-menu {
        width: 80%;
    }
}
</style>

<script>
    // Toggle mobile menu
    const hamburger = document.getElementById('hamburger');
    const navMenu = document.getElementById('navMenu');

    hamburger?.addEventListener('click', () => {
        hamburger.classList.toggle('active');
        navMenu?.classList.toggle('active');
    });

    // Close menu when clicking on a link
    const navLinks = document.querySelectorAll('.nav-link');
    navLinks.forEach(link => {
        link.addEventListener('click', () => {
            hamburger?.classList.remove('active');
            navMenu?.classList.remove('active');
        });
    });

    // Close menu when clicking outside
    document.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (!target.closest('.nav-container')) {
            hamburger?.classList.remove('active');
            navMenu?.classList.remove('active');
        }
    });
</script>